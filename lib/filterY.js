module.exports = function filterY(arr) {
    const filteredWords = arr.filter(function(word) {
        return /^Yy/.test(word);
    });

    //filter and map to new array if not containing y or Y
    
    //for each to construct the sentence
    
    return filteredWords;
        
};

